# audit-service

Owns immutable audit event ingestion, retention policy boundaries, and regulated activity query surfaces.

Initial implementation must reject unauthenticated audit writes and redact sensitive values.

