# transaction-service

Owns transaction commands, idempotency, saga orchestration, posting state, and customer transaction history APIs.

Initial implementation must define transaction state transitions before any money movement code is introduced.

