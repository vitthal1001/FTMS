# analytics-service

Owns operational projections, customer insights, and platform analytics read models.

Initial implementation must consume published events rather than querying service-owned transactional databases.

