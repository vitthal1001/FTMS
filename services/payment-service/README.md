# payment-service

Owns payment provider adapters, external rail retries, provider idempotency mapping, reconciliation hooks, and payment status events.

Initial implementation must isolate provider integrations behind ports and adapters.

